package com.opencbs.core.domain.enums;

public enum SystemSettingsName {

    PASSWORD_LENGTH,
    UPPER_CASE,
    NUMBERS,
    FIRST_LOG_IN,
    EXPIRE_PERIOD,
    EXPIRATION_SESSION_TIME_IN_MINUTES,
    DATE_FORMAT,
    TIME_FORMAT,
    NUMBER_FORMAT
}
